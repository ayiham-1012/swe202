## Object Oriented
- Class-based language
- 4 Fundamental Pillars
  - Encapsulation ==> "Data Hiding" ==> `private` & `getters, setters`
  - Inheritance
    - Polymorphism
  - Abstraction

## Platform Independent / Cross-Platform
- C: Platform Indepened? Yes
- Java: Bullshit Language, for java, this is simply just a marketting term

## Compiler
- `javac` = java compiler,     kni27.java --> `javac` --> kni27.class
- .java file looks almost similar to .class file
  - `javac` is not giving us binary
  - `javac` is     giving us `.class`
  - `javac` is     giving us byte-code
    - Bytecode is a universal format
      - This is another marketting Term
  - `gcc` gives us binary
 
## Interpreter
- `.py` 
- `python kni27.py` 
  - 1 line after 1 line gets executed
  - kind of like a settings file
  - maybe even, 1 line 1 line kore binary te convert hoy.
    </br>
- `java kni27`
  - `<JVM> runs kni27.class`
  - `JVM` converts `.class` into MachineBinaryCode

## Type-Casting / Type-Conversion
```c
int x = 4;
float y = (float) x;     // type-casting
```

## Java is Strongly Typed
- Java requires us to always explicitly use type-casting.
- Strongly Typed = "explicit casting req."
```c
int x = 4;
float y = x;     // will not work in a Strongly-Typed language
```

## Java is a Compiler-interpreter language
1.
2.
3.








## Entry-Point
```java
public class a4q3 {
    public static void main(String[] args){
    }
}
```

## Access Modifier
- `public`
- `private`

## Reference & Pointers
- 2 Sides of the same coin

## Glossary
- Method = Function = Behavior
- Object = Instance
- Variable inside a Class = Member = Data = State = Attributes = Field
- Instantiation
  ```java
    Student s1 = new Student();
    Student s2 = new Student();
  ```
- new ==> Allocated memory
- s1, s2 ==> Reference/Pointer Variable
- s1, s2 ==> 8Byte = 64bit er ekta Memory-Adress holder

- "Each Object is independent"
  - "Each of them occupy different pieces of RAM-Memory"
  - s1.name & s2.name are in different parts of RAM
  - s1.name & s2.name can have different values @ a exact moment of time.

- Method Invocation = "Calling a function"

## Constructor


## Static
```java
public class Account {
    static String bankName = "Badda Bank";
    static int count = 0;
    static double totalBalance = 0.0;

    // BluePrint doesn't occupy memory ====> this line is false;

    String name;
    String addr;
    double bal;
}
```
```java
    a1.name;                // 800 can hold "Abir"
    a2.name;                // 900 can hold "Mira"
    Account.BankName;       // 700
    a1.BankName;            // 700
    a2.BankName;            // 700
    a1.BankName = "Dhaka";  // 700 memory address change
```

