echo "# swe202" >> README.md       // lagbena
git init                           // [initialize] git starts doing smth, creates .git

git add README.md                  // add = "start tracking"
git add .                          // add = "start tracking"
git commit -m "first commit"
git commit

git branch -M main
git remote add origin https://github.com/ayiham-1012/swe202.git     // remote add <"smth" = "upstream"> <link>
git push -u origin main            // initial push, -u <kon-upstream> <kon-branch>


# nice ones
git log

# For later on
git add .       // add = "add all files to commit"
git commit      // commit = "record"
git push        // push = "upload"